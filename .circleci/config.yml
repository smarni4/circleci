# CircleCi configuration file

version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo "Hello world!"
            echo 'This is the delivery pipeline'
      - run:
          - name: The Second Step
            command: |
              echo "The files in the project repo"
              ls -la